<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ahmed.cvmaker">

    <!-- for camera and gallery -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!--for the web view -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- to get current user location using GPS in android -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.hardware.camera.autofocus" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PersonalDataActivity"></activity>
        <activity android:name=".ProfileActivity"></activity>
        <activity android:name=".EducationalActivity"></activity>
        <activity android:name=".ProjectDetailsActivity"></activity>
        <activity android:name=".ExperienceActivity"></activity>
        <activity android:name=".UploadPhotoActivity"></activity>
        <activity android:name=".ReferenceDetailsActivity"></activity>
        <activity android:name=".GenerateResumeActivity">

        </activity>
        <activity android:name=".OtherDetailsActivity"></activity>
        <activity android:name=".ExistingEducational"></activity>
        <activity android:name=".ExistingExperience"></activity>
        <activity android:name=".ExistingProjects"></activity>
        <activity android:name=".ExistingReferences"></activity>


    </application>

</manifest>